import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:dio_app/app/modules/home/subpages/create/create_module.dart';

void main() {
  initModule(CreateModule());
  // CreateController create;
  //
  setUp(() {
    //     create = CreateModule.to.get<CreateController>();
  });

  group('CreateController Test', () {
    //   test("First Test", () {
    //     expect(create, isInstanceOf<CreateController>());
    //   });

    //   test("Set Value", () {
    //     expect(create.value, equals(0));
    //     create.increment();
    //     expect(create.value, equals(1));
    //   });
  });
}
